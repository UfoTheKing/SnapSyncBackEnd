import { Request, Response, NextFunction } from 'express';

class LocationsController {
  public locationSearch = async (req: Request, res: Response, next: NextFunction) => {};
}

export default LocationsController;
